apiVersion: infrastructure.cluster.x-k8s.io/v1beta2
kind: DevMachineTemplate
metadata:
  name: baro-worker-image-template
  namespace: default
spec:
  template:
    spec:
      backend:
        docker:
          preLoadImages:
            - k8s-monitoring-lab:dev
          extraMounts:
            - containerPath: /var/run/docker.sock
              hostPath: /var/run/docker.sock
